{
  "version": 2,
  "buildCommand": "cd kenyahouselistings && npm install && npm run build",
  "outputDirectory": "kenyahouselistings/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
